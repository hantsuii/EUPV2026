# 2026-09-23

- ODP 发运分析（modules/odp/）：综合到货分析 → 港口到货分析新增 SKU 多选筛选（带搜索、全选/清空），与既有港口筛选同款交互。
  - 改动文件：odp-shipping-analysis.html（新增 .port-sku-filter 容器）、odp-shipping-analysis.js（portSkuFilterSelection 状态、availablePortSkuOptions/commitPortSkuFilter/renderPortSkuFilter、buildPortDailyData 应用 SKU 过滤、数据类型切换与重新加载时重置）、odp-i18n.js（skuFilter/skuSearch/allSkus 中英文）。
  - 机制：SKU 选项来自当前数据类型下有效到货记录；空 SKU 归入 __UNKNOWN__ 显示"未填写"；导出 Excel 与图表/表格共用 buildPortDailyData，自动遵循筛选。
  - 验证：node --check 语法通过；DOM 桩冒烟测试 11 项断言全过（渲染、部分/全选提交、数据过滤、归一化）。
  - 注意：本机 Git Bash 环境 PATH 异常（ls/grep 等 coreutils 不可用），用绝对路径 node/python 或专用工具替代。
