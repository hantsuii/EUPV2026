# 2026-09-24

- ODP 发运分析（modules/odp/）：综合到货分析 → 港口到货分析将 SKU 筛选改为 Model 筛选（与综合到货分析一致），柱状图按 Model 堆叠着色，表格每个港口下分 Model 子行+港口小计行，Excel 导出同步改 port×Model 行。
  - 改动文件：odp-shipping-analysis.html（.port-sku-filter→.port-model-filter）、odp-shipping-analysis.js（portSkuFilterSelection→portModelFilterSelection、availablePortSkuOptions→availablePortModelOptions/commitPortSkuFilter→commitPortModelFilter/renderPortSkuFilter→renderPortModelFilter、删除 skuKey/skuLabel、buildPortDailyData 返回 Map<port,Map<day,Map<model,containers>>> 嵌套+按 Model 过滤、renderPortDailyChart 改为按 Model 堆叠+图例、renderPortDailyTable 改为 Model 子行+小计行、exportPortDaily 改 port×Model 行、全局点击代理 class 判断同步更新）、odp-i18n.js（skuFilter/skuSearch/allSkus 文案改为 Model 相关）。
  - i18n key 沿用 skuFilter 复用（因复用 selectedModels 模板），文案改为"Model 筛选/全部 Model"。
  - 验证：node --check 语法通过；DOM 桩冒烟测试 11 项断言全过（Model 下拉、嵌套数据结构、部分/全选过滤、表格子行+小计行、modelKey 归一化）。

- 港口到货分析修复两个问题：
  1. x 轴标题“实际开船日期”→“到货日期”：新增 i18n key hArrivalDate（zh:到货日期 / en:Arrival Date），renderPortDailyChart 中 `t("atdDate")` 改为 `t("hArrivalDate")`。航线P90趋势图的 atdDate 保留不变（语义正确）。
  2. 港口/Model 筛选提交后筛选框摘要不更新：根因是 commitPortFilter/commitPortModelFilter 后只调了图表/表格渲染，没有重新渲染筛选框。修复：portDailyApply 按钮、下拉内“应用”按钮提交后都加调 renderPortDailyFilters()，下拉内提交还自动收起菜单。
  - 验证：node --check 语法通过；DOM 桩 7 项断言全过。

- 港口到货分析 Excel 导出修复：exportPortDaily 第436行 modelList 收集有 bug `[p[0]&&...]`（p 是港口名字符串，p[0] 取首字符），改为与 renderPortDailyTable 一致的 `[...data.get(p).values()].flatMap(...)`。导出结构：表头[目的港,Model,各日期...,Containers]，每个港口下按 Model 子行+Containers 小计行，与页面表格一致。vm sandbox 冒烟测试确认数据正确（ANTWERP: TCL-400(0,5,5)+TCL-550(0,4,4)+sum(0,9,9)，ROTTERDAM: TCL-400(2,0,2)+TCL-550(3,0,3)+sum(5,0,5)）。

- 可用库存分析（modules/stock/）：在途日期范围从硬编码改为动态——开始日期自动取当天，结束日期取当天+6个月；展示截断到最后的到货活动日期。
  - 新增函数：todayISO()、addMonthsISO(startDateISO, months)、setDefaultTransitDates()、findLastActivityDateIndex(dateHeaders)。
  - HTML：available-stock-analysis.html 删除 transitStart/transitEnd 的 value 硬编码（"2026-08-01"/"2026-12-31"）。
  - JS：available-stock-analysis.js 页面加载时调用 setDefaultTransitDates()；resetForm() 改为动态日期；所有 fallback 默认值改为 todayISO()/addMonthsISO()；renderChartAndTable() 中计算 inRangeHeaders 后用 findLastActivityDateIndex 截断为 displayHeaders，图表和明细表渲染均用 displayHeaders。
  - Python：inventory_step1_to_stock.py 的 TRANSIT_START_DATE/TRANSIT_END_DATE 改为 None + _default_start()/_default_end() 动态解析；CLI --transit-start/--transit-end 默认改为 None。